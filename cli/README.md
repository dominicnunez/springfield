# sfk CLI

Canonical documentation lives in the repository root README:

- [/home/dom/Repos/springfield/README.md](/home/dom/Repos/springfield/README.md)

For local development, run `cd cli && bun install` to install dependencies and Husky hooks.

Engine effort follows `~/.sfk/config`:
- Claude: `low`, `medium`, `high`
- Codex/OpenCode: `low`, `medium`, `high`, `xhigh`

Unsupported effort values fail fast before execution.
